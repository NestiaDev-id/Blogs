[package]
name = "backend"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "hello"
path = "api/hello.rs"

[dependencies]
tokio = { version = "1", features = ["full"] }
vercel_runtime = { version = "2.4", features = ["axum"] }
serde_json = "1.0"
